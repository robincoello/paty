#!/usr/local/bin/latino

cmd = "echo 'hola mundo'"
latino_version = sis.pipe('latino -v')


//salida = ejecutar_pipe(cmd)

salida = sis.pipe(cmd)

escribir("salida: ")
escribir(latino_version)
//escribir("---------------------------------------------------")
//escribir("*************************************************")
//escribir(salida)

i = 0
mientras i < 5
    escribir("Esto es i: "..i)
i++
fin




















